# For Streamlit Community Cloud deployments only.
# In the app dashboard: Settings -> Secrets, paste the contents below
# (with your real key) — do NOT commit an actual secrets.toml file.
OPENROUTER_API_KEY = "your_openrouter_api_key_here"
